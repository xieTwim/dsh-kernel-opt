# The host's preset metadata reads `name` / `description` / `order` and nothing
# else (dsh packages/preset/agent-presets/src/metadata.ts) — there is no locale
# key here, so whatever this file says is what every interface language shows.
# The picker it lands in is the one carrying 标准模式 / PTC 模式 / 极简模式 /
# 创造模式, so an English name here reads as the one row that does not belong.
name: 算子优化模式
description: 具备标准模式的全部能力，专注 GPU 算子性能优化：自动搭建评测、持续迭代，并实时展示优化曲线、最优结果和监督记录。
order: 30
